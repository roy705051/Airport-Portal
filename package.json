{
  "name": "new_airport",
  "version": "1.0.0",
  "description": "",
  "main": "schedule2.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "json-server --watch data.json --port 8080"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "json-server": "^0.17.3"
  }
}
